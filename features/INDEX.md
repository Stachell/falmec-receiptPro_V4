| ID | Feature Name | Status | Description |
| :--- | :--- | :--- | :--- |
| PROJ-0 | Base Setup & Onboarding | Done | Initial import of Lovable codebase with local Vite/React workflow and project scaffolding. |
| PROJ-1 | Dashboard & Run Archive | In Progress | Dashboard table for runs with status, issue count, direct downloads, archive dialog, and run log access. |
| PROJ-2 | New Run Intake & File Upload | In Progress | Upload of required source files (invoice/open orders/serial list/article master) and start of a new processing run. |
| PROJ-3 | Run Detail Workflow Cockpit | In Progress | Stepper, KPIs, tabbed run analysis (details, invoice preview, items, issues, warehouse, export). |
| PROJ-4 | Invoice PDF Parsing Engine | In Progress | TypeScript invoice parser with pdfjs extraction, warning model, and optional devlogic API fallback parser. |
| PROJ-5 | Issue Management & Resolution | In Progress | Filtering, resolving, and CSV export of run issues with blocking vs warning severity handling. |
| PROJ-6 | Warehouse Location Assignment | In Progress | Global and per-line storage-location assignment with validation for missing locations before export. |
| PROJ-7 | Export Generation (XML/CSV/JSON) | In Progress | XML preview/export plus run-level CSV/XML/JSON download actions for downstream ERP import. |
| PROJ-8 | Logging, Archiving, and Snapshots | In Progress | Per-run/system logs, archive folder model, snapshot generation, and file download helpers. |
| PROJ-9 | Local Persistence & Filesystem Integration | In Progress | localStorage + IndexedDB persistence and File System Access API integration for local folder workflow. |
| PROJ-10 | QA Baseline (Unit Tests) | In Progress | Vitest baseline with parser/config/order-tracker tests and jsdom test setup for pdfjs. |
| PROJ-11 | Data-Matching-Update | Open | Neue Feldstruktur (Umbenennung, Checkbox 5-Zustaende, Preis-Konsolidierung), Bestellparser mit Matching-/Fallback-Logik, Invoiceline-Expansion, Export-Mapping 10 Spalten. |
| PROJ-12 | Advanced Logging & File-System Brain | In Progress | Hybride Logging-Architektur: Run-Logfile (tiefes Parser-Tracking), Home-Logfile (globales System-Log mit Rotation), Archivierungs-Paket (gebuendelter Run-Export auf Festplatte), localStorage-Cleanup. Baut auf PROJ-8/PROJ-9 auf. |
| PROJ-13 | Log-Toolbar Export-Buttons & Follow-Redesign | Done | Kopieren/Download-Buttons mit buildLogText()-Helper, Follow-Button auf Circle-Icon mit rotem Pulse-Effekt umgestellt. Siehe `PROJ-13-log-toolbar-export-follow.md`. |
| PROJ-14 | Parser-Modularisierung & UI-Bereinigung | In Progress | Modulare Parser-Architektur (Modul-Ordner, V1/V2, Import-Funktion), Settings-Popup (ausgelagerte Slider-Controls), Parser-Dropdown, Home-Log/Issue-Center fuer Parser-Events, systemweite Umlaut-Korrektur. Baut auf PROJ-4/PROJ-12 auf. **Phase A+B (UI) erledigt.** |
| PROJ-15 | Run-Detail UI & Logic Fixes | In Progress | Kachel-Label/Zaehler-Fixes, Tab-Umbenennung, Badge-Counter, Popup-Crash-Fix (shadcn Exit-Animation), Details-Tab Cleanup + LINK-Bereich, Toast Race-Condition KISS-Fix. Baut auf PROJ-14 auf. |
| PROJ-16 | ETL-Pipeline & S/N-Verifizierung | In Progress | Modulares Matcher-System (analog ParserRegistry) fuer Step 2 (Cross-Match) und Step 3 (S/N-Extraktion). FalmecMatcher_Master, Schema-Alias-System, S/N-Regex-Pipeline, Matcher-Dropdown + Settings-UI. Baut auf PROJ-14/PROJ-15 auf. |
| PROJ-18 | Run-Detail UI-Refinement | Done | Symmetrische Abstaende (KPI<->Tab-Leiste, Tab-Leiste<->Body je 24 px). Expand/Collapse-Logik (ChevronsDown/Up, animate-pulse, max-h-Transition 500 ms) fuer RE-Positionen, Artikelliste, Lagerort-Details und XML-Vorschau. Spalten-Rename in Artikelliste: DE->Art-# (DE), Artikel-# (IT)->Art-# (IT). Nachtrag 2026-02-24: Viewport-fit collapsed Hoehe, Sticky Header nur collapsed, neue Headerlabels in der Artikelliste. |
| PROJ-19 | Stammdaten-Infrastruktur | Done | Excel-Upload â†’ IndexedDB â†’ Matcher Pipeline. masterDataParser.ts (SheetJS, Schema-Alias-Filterung, Collision-Resolver), masterDataStore.ts (IndexedDB-Persistenz, localStorage-Metadaten), Pre-Explosion Matching, App.tsx Boot-Hydration. Siehe `PROJ-19-stammdaten-infrastruktur.md`. |
| PROJ-20 | SerialFinder + OrderMapper (Waterfall-4) | Done | SerialFinder (preFilteredSerials, Lean Archive, Cache Cleanup). OrderMapper: 4-stufiger Waterfall (Perfect â†’ Reference â†’ SmartQty â†’ FIFO), Sortierung year/belegnummer ASC. OrderParser fuer Excel/CSV openWE. Parsing-Rules Dokumentation. Siehe `PROJ-20-serial-finder-order-mapper.md`. |
| PROJ-21 | Zentrales Issue- & Log-Center | Done | Phase 1: Severity-Migration (blocking/soft-fail â†’ error/warning/info), 3 neue IssueTypes, Issue.context. Phase 2: Issue-Generatoren (price-mismatch, inactive-article, serial-mismatch, order-*). Phase 3: Jump-Links (navigateToLine, highlightedLineIds, scrollToLineId, auto-clear 5s). Phase 4: Auto-Resolve + run-report.json. 4 Hotfixes (Global State Leak, Retry-Flow, Status-Kaskadierung, Log-Buffer). Siehe `PROJ-21-issue-log-center.md`. |
| PROJ-22 | Enterprise UI/UX Polish | Open | Massives Frontend-Redesign: Sticky-Headers, KPI-Kacheln 20% kleiner, Tabellen-Sync (RE-Positionen & Artikelliste identische Spaltenreihenfolge), differenzierte READ-ONLY Regel (Preise nur in RE-Pos, Bestellungen nur in Artikelliste), DetailPopup Redesign (invertierte Farben, S/N-Dropdown, Felder-Umordnung), Settings-Popup mit 6-Tab-Layout, Archive-Seite mit neuen Spalten + Suchleiste. Rein visuell/CSS/Komponenten - kein Backend-Code. Nachtrag 2026-02-26: RE-Details-Icon oeffnet eigenes Popup (`Artikeldetails Rechnungszeile`) statt Tab-Switch; Placeholder `--` in beiden Detail-Popups auf Pending-Sanduhr vereinheitlicht. Siehe `PROJ-22-23-MASTERPLAN.md`. |
| PROJ-23 | Architecture Pivot: State-Persistenz + Article-First OrderMapper | Open | Fundamentaler Architektur-Pivot: (A1) Aggregated-First Line Model - Step 1 bewahrt aggregierte Zeilen (qty>1) statt sofortiger Expansion. (A2) IndexedDB Persistenz mit Auto-Save, Speicher-Waechter und File System Access API Export. (A3) Article-First OrderPool - nur Bestellungen fuer Rechnungsartikel geladen, Soft-Fail Validierung. (A4) 3-Run Matching-Engine: Perfect Match + Partial/Fillup (aggregiert) -> Expansion + FIFO (Einzelzeilen). (A5) ManualOrderPopup mit bidirektionalem Pool-Tracking (consume/return). Ersetzt PROJ-20 Waterfall-4. Siehe `PROJ-22-23-MASTERPLAN.md`. |
| PROJ-24 | Run Stability: Modular Order-/Serial-Flow | In Progress | Settings-gesteuerte Stabilisierung von Step 3/4: konfigurierbare Order-Parser-Profile (Alias-Overrides), scorebasierte Spaltenwahl mit Tie-Break (Belegnummer-Prioritaet), Diagnostics + Confidence-Gate fuer Step 4, strict-failure Regel fuer serialRequired in Step 3. Keine Aenderungen an bestehenden Run-Detail-Feedback-Labels. Siehe `PROJ-24-run-stability-modular-order-serial.md`. |
| PROJ-26 | Kachel-Anpassung-Codex | In Progress | Run-Detail Kacheln: Label-Umbenennung "Beleg zugeteilt", pausierte Export-Kachel ohne Layout-Shift, Analyse der Counter/Nenner-Prueflogik (Step 1-4), Weitergabe/Anzeige der Rechnungssumme aus Parser-Footer, technische Kachel-Dokumentation in `features/Kachel-Rules/`. |
| PROJ-27 | Site Settings / Einstellungen | In Progress | Settings-UI Rework: Logfile-Bereich in Allgemein (inkl. Inline-Label `Logfile (global) anzeigen:`), Speicher/Cache-Separatoren, Entfernen des Reiters `sonstiges`, Footer-`Datenverzeichnis` im Einstellungen-Button-Hoverdesign, Ersatz der Footer-Selects (`Parser-Regex`/`Matcher`) durch kompakte Statusfelder (PDF-Parser, Art.-Matcher, Serial-Finder, OrderMapper), Deep-Links von den 4 Statusfeldern, `Settings`-Start auf Reiter `Allgemein`, weisser Grundzustand der 4 Status-Linkbuttons und dokumentierte Loeschbarkeitspruefung zu `sonstiges`. Siehe `PROJ-27-site-settings-einstellungen.md`. |
| PROJ-28 | Unification of Settings & Workflow Mechanics | Done | Standardisierung aller 4 Step-Tabs (einheitliche Abschnitte Aâ€“F), Block-Step-Toggles (Step 2 + Step 4), OverrideEditorModal (Alias + Regex getrennt), unifizierte StepDiagnostics in RunState, Migration lastOrderParserDiagnostics. Siehe `PROJ-28-settings-unification.md`. |
| PROJ-29 | Run-Detail KPI Double-Check Logic & Line 3 Remapping | In Progress | Neue subValues (Zeile 3) fÃ¼r Kacheln 1, 2, 4, 5. Reaktive Double-Check-Logik (useMemo) pro Kachel 1â€“5: bei Erfolg grÃ¼ne Kachel (`bg-emerald-100/90`) + CheckCircle2-Icon. Rein UI-seitig, kein Einfluss auf Workflow. Kachel 6 unberÃ¼hrt. Siehe `PROJ-29-kpi-double-check.md`. |
| PROJ-30 | Optische Aufwertung Tab-Reiter-Auswahl | Done | 3D-Relief-Effekt fÃ¼r Tab-Leiste im Run-Detail: Bar erhaben (outset shadow), aktiver Tab eingedrÃ¼ckt (inset shadow). Rein CSS, keine Logik-/FarbÃ¤nderungen. Siehe `PROJ-30-tab-reiter-aufwertung.md`. |
| PROJ-31 | Artikelliste Lock/Unlock Icon | Done | Visuelles Schloss-Icon im Artikelliste-Header ersetzt den bisherigen Zaehler-Text. Lock (grau) wenn Schritt 4 noch nicht abgeschlossen, Unlock (gruen) wenn Step 4 status='ok'|'soft-fail'. ADD-ON 6: Umstellung auf lokale `.ico`-Assets (`Lock_CLOSE_STEP4.ico` / `Lock_OPEN_STEP4.ico`), schwarzer Hintergrund bleibt entfernt. Rein visuell, kein Click-Handler. Siehe `PROJ-31-article-list-lock-icon.md`. |
| PROJ-32 | Bestellung Match-Status Button | Done | Farbiger Hintergrund-Pill in der Bestellung-Spalte der Artikelliste zeigt visuell den OrderAssignmentReason an (Teal=Erfolg, Blau=Sekundaer, Amber=Fallback, Violett=Manuell, Grau=Ausstehend, Rot=Nicht bestellt). Rein visuell, nur Tailwind-Klassen. Siehe `PROJ-32-bestellung-match-status-button.md`. |
| PROJ-33 | Run-Detail Match-Icon Unification + Pending Hourglass | Done | `-MATCH` in Artikelliste und RE-Positionen auf dieselbe `matchStatus`-Quelle vereinheitlicht, neue Icon-Zuordnung (`Code_IT.ico`, `EAN.ico`, `no-match = X`), animierte Pending-Sanduhr (`hourglass base + overlay` mit Opacity-Puls), plus pending-Visual in PREIS/CHECK angeglichen (finaler Background `#968C8C`). Siehe `PROJ-33-run-detail-match-icon-unification.md`. |
| PROJ-34 | Icon-Guide Popup | Done | Legende-Popup fuer alle Run-Detail-Icons (Match, Preis, Serial, Bestellung). SSOT: Echte Komponenten/Helper. Neuer Button im AppFooter. SerialStatusDot-Extraktion als SSOT-Refactor. Siehe `PROJ-34-icon-guide-popup.md`. |
| PROJ-35 | Export-Konfiguration & Spalten-Reihenfolge | Done | Neuer Tab â€žExport" im SettingsPopup mit 15-Spalten-Sortierliste (Pfeil-Buttons), exportConfigStore (Zustand + localStorage), konfigurierbare XML-Spaltenreihenfolge in ExportPanel, Export-Diagnostics, AppFooter-Statusfeld â€žExport". Layout-Shift-Fix (h-[65vh]/max-h-[800px]) auf Tabs-Container. |
| PROJ-36 | Click-to-Copy fuer Tabellenwerte im Run-Detail | Done | KISS-Variante mit 8 Ziel-Locations umgesetzt (RE-Positionen: ARTIKEL, BESTELLNUMMER, EAN, BESTELLUNG; Artikelliste: ARTIKEL, BESTELLNUMMER, EAN, SN/SERIAL). Render-Isolation via `CopyableText` Micro-Komponente, Artikelliste-BESTELLUNG und ManualOrderPopup unveraendert. Siehe `PROJ-36-click-to-copy-tabellenwerte.md`. |
| PROJ-37 | Fehler-Center Optimierung | Done | affectedLineIds UI-Rendering, IssueCard Collapsible-UI (Header/Body/Footer), aktiver Issue-Filter beim Tab-Wechsel (activeIssueFilterIds → Artikelliste/RE-Positionen), FilterX-Reset in allen 3 Tabellen (Artikelliste, RE-Positionen, Fehler-Center), useCopyToClipboard-Hook (PROJ-36 Extraktion), Display-Text-Umbenennung Issue→Problem, Banner-Kontrast-Fix. 12 Dateien, 7 Phasen. Siehe `PROJ-37-fehler-center-optimierung.md`. |
| PROJ-38 | Filterzeile Optimierung & RE-Positionen Action-Filter | Done | Artikelliste: 16 Filter in 5 Gruppen (ARTIKEL/PREIS/SERIAL/BESTELLUNG) mit SelectGroup-Rendering, Null-Safety. RE-Positionen: Neuer Action-Filter (4 Optionen: Preisabweichungen/Matchstatus/Bestellung fehlt/Konflikte) mit Hybrid-Logik (PDF + enriched). DRY via shared `filterConfig.ts`. 1:1 UI-Klon (L1). Nachtrag 2026-03-02: offene Suche in beiden Tabellen auf zentrale `normalizeSearchTerm`-Normalisierung umgestellt, Dropdown-Filter unveraendert. Siehe `PROJ-38-filterzeile-optimierung.md`. |
| PROJ-22-23-ADD-ON | Fertigstellung POPUP „PREIS / CHECK" mit Verdrahtung | Open | ADD-ON zu PROJ-22 Phase B3: Verdrahtung `handleSetPrice` → `setManualPrice` Store-Action. Preis-Popup ist UI-fertig, läuft aber ins Leere. Verhalten vor/nach Step 4 (isExpanded): vor Step 4 direkter Popup-Aufruf, nach Step 4 Jump zur Artikelliste + Popup in Artikelliste. `readOnly`-Logik in ItemsTable dynamisch. Siehe `PROJ-22-23-ADDON-PriceCheck-Verdrahtung.md`. |
| PROJ-23-ADD-ON-upload-validation | Upload-Validierung & Dateiformat-Erweiterung | In Progress | ADD-ON zu PROJ-23: XLSX/XLS/XML-Support für Order-Upload (umgeht CSV-Präzisionsverlust bei EANs), Fail-Fast Pre-Check (wissenschaftliche Notation + fehlende IDs), KISS-Bugfix Warning-State auf NewRun-Seite. Siehe `PROJ-23-ADDON-upload-validation.md`. |
| PROJ-23-ADD-ON-manual-popup-info | ManualOrderPopup — PDF-Kontext-Info-Zeile | In Progress | Reine Read-Only UX-Ergänzung: Neue Info-Zeile "PDF übermittelt: [Wert]" im Bestellung-zuweisen-Popup. Datenquelle: parsedPositions[positionIndex].orderCandidatesText (persistenter Step-1-Rohwert, nie überschrieben). Keine Logik-Änderung. Siehe `PROJ-23-ADDON-manual-popup-pdf-info.md`. |
| PROJ-40 | IndexedDB Optimierung & Datenmodell-Erweiterung | Done | 7-Phasen-Implementierung: (1) Foundation: ArticleMaster +descriptionDE/supplierId, ExportColumnKey qty->supplierId + eingangsart->orderVorgang, PersistedRunData +parsedInvoiceResult/serialDocument/uploadMetadata, OrderParserFieldAliases +vorgang. (2) Parser & Matcher: FALMEC_SCHEMA Aliases, masterDataParser Extraktion, 3-Blocker Fail-Fast Lieferant (severity:error), descriptionDE/supplierId Durchreichung, Vorgang-Aliases + Validierung. (3) Export-Spalten: exportConfigStore + ExportPanel resolveColumn. (4) UI: BEZEICHNUNG DE/IT Toggle (Shadcn Switch) in ItemsTable. (5) IndexedDB Payload: AutoSave vollstaendig (parsedInvoiceResult/serialDocument/uploadMetadata), descriptionIT-Truncation (10 Zeichen, Memory unveraendert), loadPersistedRun vollstaendige Rehydrierung. (6) Run-Isolierung: currentParsedRunId Guard gegen parsedPositions-Bleed + URL-Fallback in RunDetail (IndexedDB-Nachladen bei Direktaufruf). Alle Phasen: tsc 0 Errors. Siehe PROJ-40_IndexedDB_Architekturplan.md. |
| PROJ-40-ADD-ON | Bugfix: Lieferant-Blocker + Alias + RE-Positionen Toggle | Done | 3 Bugs nach PROJ-40 behoben. Bug 1: Blocker 2+3 (severity:error) aus crossMatch entfernt; per-Line Supplier-Validierung nach Match-Loop als severity:warning eingefügt (IssueType 'supplier-missing' ergänzt). Bug 2: descriptionDE-Aliases erweitert um 'Matchcode_Artikel', 'Matchcode Artikel', 'Artikelbezeichnung', 'Bezeichnung', 'Kurztext', 'Artikelname', 'Langtext'. Bug 3: DE/IT-Toggle (Shadcn Switch + showDE State) in InvoicePreview.tsx eingebaut; Zell-Rendering zeigt descriptionDE via posStatus.representativeLine mit IT-Fallback. tsc 0 Errors. |
| PROJ-40-ADD-ON-2 | Bugfix: Step-3-Serienpersistenz (Neu verarbeiten + Archiv-Reload) | Done | 3 chirurgische Fixes in `runStore.ts`: Fix A — Step-4-Cleanup (preFilteredSerials+serialDocument löschen) entfernt, damit "Neu verarbeiten" und archiveRun() noch auf Cache-Daten zugreifen können. Fix B — `serialDocument` wird beim serialList-Upload via Konvertierung `PreFilteredSerialRow[]→SerialDocumentRow[]` befüllt und via bestehendem AutoSave-Mechanismus in IndexedDB persistiert; Import `SerialDocumentRow` aus `@/services/matchers/types` ergänzt. Fix C — `consumed`-Flags werden vor jedem Legacy-Matching (`matcher.serialExtract`) auf `false` zurückgesetzt, um persistierte consumed:true-Werte aus vorherigen Runs zu neutralisieren. tsc 0 Errors. Siehe `Step4-Serial_Indexed_persis_diganostic_NEW.md`. |
| PROJ-40-ADD-ON-3 | Serial-Persistenz Finalisierung (Hard Checkpoint + Unmount-Flush) | Done | Zwei-Pfeiler-Lösung gegen Race-Condition beim Wegnavigieren nach Step 3. Pfeiler 1: Hard Checkpoint in `executeMatcherSerialExtract` (SerialFinder + Legacy-Pfad) — direkter awaited `runPersistenceService.saveRun()` nach dem state-set. Pfeiler 2: Unmount-Flush in `useRunAutoSave` — pending Debounce-Timer wird bei Cleanup statt abgebrochen sofort ausgeführt (fire-and-forget, via `lastRunIdRef`). Shared Helper `buildAutoSavePayload.ts` extrahiert Payload-Konstruktion (DRY). `executeMatcherSerialExtract` Signatur auf `async` geändert (abwärtskompatibel). tsc 0 Errors. Siehe `PROJ-40-ADDON-3_Serial_Persistenz_Final.md`. |
| PROJ-27-ADD-ON | Archiv & Speicher Hygiene | Done | 4 chirurgische Pillars ausschliesslich in `SettingsPopup.tsx`. Pillar 1: Selective Clear — `handleClearCache` löscht nur volatile Keys (falmec-uploaded-files, falmec-parsed-invoice, falmec-system-log, falmec-log-snapshots, falmec-run-log-*), Settings + Archiv-Keys bleiben erhalten. Pillar 2: Diagnosefenster im Tab Speicher/Cache — Archiv-Pfad (fileSystemService.getDataPath), Run-Count (loadRunList), letzter Export (localStorage falmec-archive-stats). Pillar 3: Dropdown-Button "Archiv ablegen" — Option 1: Export + alte Runs >12 Monate löschen (exportToDirectory(12)), Option 2: Komplettes Archiv erzwingen mit Safety-Check (exportedCount >= runList.length — alle Runs, nicht nur completed) + Force-Bestätigungsdialog. Pillar 4: Manueller Import — Pfad A (run-data.json → saveRun), Pfad B (metadata.json+invoice-lines.json → rekonstruierter PersistedRunData). tsc 0 Errors. Nachgelieferter Fix: Safety-Check-Bedingung von completedRuns.length auf runList.length korrigiert (verhindert Teilverlust bei unfertigem Export). UI-Refactoring (2026-03-07): Tab-Reihenfolge optimiert (Archiv ablegen → Diagnose → Import → Cache leeren), DropdownMenu → Select+Button (archiveRetention State: 6/12/all, Default 6 Monate), dynamische Beschreibungstexte, handleArchiveDefault(months) parametrisiert, AlertDialog-Text aktualisiert. Siehe `PROJ-27-ADDON_Archiv_Speicher_Hygiene.md`. |
| PROJ-41 | UI- & Log-Verkabelung | Done | Chirurgisches Wiring von Business-Logik, UI (Fehlercenter/Settings) und Logging. Phase 1: affectedLineIds auf serial-mismatch (1A, war bereits gesetzt), Step-3 Diagnostics für preFiltered-Pfad (1B, bereits), Anti-Cheat-Toggle blockStep4 entfernt (1C, bereits), Parser-Issues bei retryStep+resumeRun durchleiten (1D, 2 Stellen neu), S/N-Mismatch WARN/ERROR + Upload-runId (1E). Phase 2: 6 manuelle Aktionen (setManualPrice/setManualOrder/confirmNoOrder/reassignOrder/resolveIssue/escalateIssue) mit logService+addAuditEntry verdrahtet (2A), Run-Log IndexedDB-Persistenz über restoreRunBuffer+PersistedRunData.runLog+useRunAutoSave (2B), console.log→logService.debug in matchingEngine.ts, console.error aus 4 Catch-Blöcken entfernt (logService.error bereits vorhanden), matcher-not-found→logService.error (2C). tsc 0 Errors. Siehe `PROJ-41_Wiring_and_Logging.md`. |
| PROJ-42 | Export Overhaul | Done | Zentraler exportService (escapeXml, csvQuote, resolveColumnValue, generateXML, generateCSV, buildExportFileName). Bug-A-Fix: vorgang in run3ExpandFifo singleAllocatedOrders (1 Zeile, chirurgisch). csvDelimiter in exportConfigStore + SettingsPopup-Dropdown. ExportPanel: XML+CSV-Download, Logging+Audit+Archive (fire-and-forget). Toolbar-Button-Fix (isExportReady useMemo, bg-[#008C99]). Index.tsx generateRunExport-Stub ersetzt, exportReady via Step-Completion-Proxy. tsc 0 Errors. Siehe `PROJ-42_Export_Overhaul.md`. |
| PROJ-42-ADD-ON | Export Bugfixes & Erweiterungen | Done | ExportColumnKey: unitPriceInvoice+unitPriceOrder → unitPrice+bookingDate. RunStats.bookingDate (einmalig beim ersten Export gesetzt). RunExportMeta+bookingDate. generateCSV+includeHeader (5. Parameter). setBookingDate-Action (Race-Condition-sicher, Return-Value). supplierId-Fallback in OrderMatcher (chosen.supplierId || line.supplierId). IssuesCenter Preis-Anzeige: unitPriceFinal. Kachel 6: teal + CSV-Download wenn allStepsComplete+isExportReady. Toolbar-Button XML+CSV entfernt. SettingsPopup: Headerzeile-Toggle (Switch, localStorage). Alle 3 generateCSV-Call-Sites aktualisiert. Punkt 12 (2026-03-08): Kachel-6-Bug gefixt — Step 5 blieb stuck in 'running', Auto-Complete-Block in advanceToNextStep ergaenzt (identisches Pattern wie Steps 2-4, runStore.ts). tsc 0 Errors. Siehe `PROJ-42-ADD_ON_Bugfixes.md`. |
| PROJ-40-ADD-ON-4 | Serial Rehydrierung Bugfix | Done | `preFilteredSerials` persistiert (IndexedDB) + rehydriert beim Laden — Kachel 4 bleibt nach Reload/Archiv-Laden korrekt. Kachel-4-Variant auf qty-basierte Memos umgestellt. 4 Dateien, tsc 0 Errors. Siehe `PROJ-40-ADDON-4_Serial_Rehydrierung.md`. |
| PROJ-40-ADD-ON-5 | Anzeigenfehler Tabelle Bezeichnung DE/IT | Done | Chirurgischer Wiring-Fix in `ItemsTable.tsx` und `InvoicePreview.tsx`: Default `showDE=true` zeigt nur DE in bestehender Primaer-Klasse; IT-Modus zeigt IT in Primaer-Klasse plus DE als graue Sekundaerzeile. Kein Fallback-Umschalten, Klassen/Layout unveraendert. `npx tsc --noEmit`: 0 Errors. Siehe `PROJ-40-ADD-ON-5_Anzeigenfehler_Tabelle_Bezeichnung_DE-IT.md`. |
| PROJ-42-ADD-ON-V | Export Versioning & Archiv Log-Fix | Done | Ziel 1 (Log-Fix): WARN in `archiveService.writeArchivePackage` filtert jetzt auf `criticalFailures` (nur invoice-lines.json / metadata.json) — keine irreführenden WARNs bei fehlendem optionalem Export mehr. Ziel 2 (Revisionssichere Versionierung): `RunStats.exportVersion` (Counter). `buildExportFileName` um optionalen `version`-Parameter erweitert (Suffix-Formel: `_v${version-1}`, erster Export ohne Suffix). `incrementExportVersion`-Action im runStore (Interface + Impl, Vorbild: setBookingDate). `archiveService.writeArchivePackage` um `extraFiles: Record<string, string>` erweitert (Schreibschleife, Legacy-Guards für exportCsv/exportXml, Metadata aus extraFileInfos). Alle 3 Handler (handleTileExport/RunDetail, handleDownload/ExportPanel, handleDownloadFormat/Index) auf incrementExportVersion + extraFiles umgestellt. tsc 0 Errors. |
| PROJ-27-ADDON-2 | Early Archive Move | Done | Behebt Browser-Sicherheitsblockade: File System Access API erfordert User Activation — Step 5 (auto, kein User Activation) scheiterte still. Neues 3-Säulen-Modell: (1) Step 1 → `writeEarlyArchive()` schreibt PDFs + Basis-`metadata.json` (status: 'running') sofort ins Archiv, User Activation vom "Run Starten"-Klick. (2) Step 5 → nur IndexedDB-Status (`updateRunStatus('ok')`) + `cleanupBrowserData()` — kein Disk-Write. (3) Kachel 6 Klick → `appendToArchive()` schreibt finale Daten (invoice-lines.json, run-log.json, serial-data.json, run-report.json, Export-Dateien) in bestehenden Ordner + überschreibt metadata.json mit finalem Stand. Amnesie-Bug-Fix: `appendToArchive` liest bestehende metadata.json per File System API, um PDF-Referenz zu bewahren. Typ-Fix: lokaler `EarlyArchiveMetadata`-Hilfstyp (Omit status+stats). 4 Dateien, tsc 0 Errors. Spec: `PROJ-27-ADDON-2_Early_Archive.md`. |
| PROJ-39-ADDON | Einstellungserweiterung Fehlerhandling | Done | Settings erweitert: neuer Tab `Fehlerhandling` (aus `Allgemein` ausgelagert), E-Mail-Persistenz auf 10 feste Slots umgestellt (stabile Rehydrierung, Legacy-5er-Migration), Speichern blockiert bei ungueltigen oder doppelten Adressen (case-insensitive), IssuesCenter-Hinweispfad auf `Einstellungen > Fehlerhandling` aktualisiert. Workflow/Deep-Links unveraendert. `npx tsc --noEmit`: 0 Errors. Siehe `PROJ-39-ADDON-Einstellungserweiterung-Fehlerhandling.md`. |
| PROJ-43 | IssueCenter Overhaul | Done | 4 Phasen: (1) Daten-Fundament: `'pending'`-Status in Issue.status, `export-no-lines` IssueType, affectedLineIds-Fix fuer price-mismatch+inactive-article (Step 2), Severity-Guard-Fix blockStep2OnPriceMismatch (Typ-Check statt Severity-Check + pending eingeschlossen), `generateStep5Issues` (missing-storage-location + export-no-lines, Auto-Resolve, Duplikat-Guard). (2) Aktualisieren-Button in IssuesCenter (refreshIssues, RefreshCw-Spin). (3) Neuer IssueDialog (5 Tabs: Uebersicht/Fehlerbericht/Loesung-erzwingen/E-Mail/Anfragen, SettingsPopup-Design 600px/bg=#D8E6E7), ersetzt alten Dialog in IssuesCenter. (4) Zustandsmaschine: escalateIssue → status:'pending', splitIssue, reopenIssue; Guards ExportPanel+RunDetail+blockStep2 auf pending erweitert; Pending-Kategorie in IssuesCenter; archiveService pendingIssues-Zaehler (beide Stellen). tsc 0 Errors. Siehe `PROJ-43_IssueCenter_Overhaul.md`. |
| PROJ-44 | Step 4 Waiting Point Switch | Done | 6 Phasen: (1) Types: autoStartStep4 in RunConfig. (2) State: isWaitingBeforeStep4/waitingStep4RunId/showStep4WaitingDialog + Actions dismissStep4WaitingDialog/proceedStep4FromWaiting. (3) Guard im t3adv-Timer (Step 3→4 Auto-Advance): wenn !autoStartStep4 → Dialog oeffnen statt advanceToNextStep. (4) RunDetail: Lock-Icon (Lock_CLOSE/OPEN_STEP4.ico) + Switch (scale-75) im Header, AlertDialog (STOP/DURCHFUEHREN). (5) Settings Allgemein: neuer Abschnitt mit Switch fuer autoStartStep4. (6) Defensive: pauseRun schliesst Dialog, setGlobalConfig synct autoStartStep4 auf aktiven Run. tsc 0 Errors. Siehe `PROJ-44-ADDON_Step4_Waiting_point_switch.md`. |
| PROJ-44-BUGFIX-R3 | Bugfix Runde 3 (Settings-Lock, E-Mail-Dropdown, Tab-Leiste) | Done | Bug #1 (Settings-Lock): `setEmailAddresses(result.addresses)` aus `handleSaveEmails` entfernt + event-driven Debounce in `handleUpdateAddress` (ersetzt useEffect-basierten Auto-Save), `isInitialMountRef` entfernt, `autoSaveTimerRef`+`emailAddressesRef` eingefuehrt. Bug #2 (E-Mail-Dropdown leer): `storedEmails` von `useMemo(fn, [])` auf `useState([])` + `useEffect([issue])` umgestellt. Bug #3 (Tab-Leiste): `h-auto` → `h-fit self-start` in IssueDialog + SettingsPopup TabsList (beide Dialoge). Debug-Logs entfernt. 2 Dateien, tsc 0 Errors. |
| PROJ-44-ADD-ON | Settings-Lock: localStorage Exception Guard | Done | Fataler UI-Crash: `localStorage.setItem()` in `saveEmailAddresses()` (errorHandlingConfig.ts) war ohne try/catch — bei vollem/blockiertem localStorage propagierte die Exception durch `handleSaveEmails` → Button gab keine Reaktion, Daten gingen verloren. Fix: try/catch um setItem, neuer `storage_error` Code in `SaveEmailAddressesResult`-Union, bestehende Caller (`result.ok` + `toast.error`) funktionieren automatisch. 1 Datei, tsc 0 Errors. Spec: `PROJ-44-ADD-ON_Settings-Lock.md`. |
| PROJ-44-BUGFIX-R2 | Bugfix Runde 2 (UI-Korrekturen) | Done | Bug #2: SettingsPopup E-Mail-Flash eliminiert (Lazy-Initializer `getStoredEmailSlots` ohne `()`), `isInitialMountRef` + Debounced Auto-Save (500ms, stilles Save). Bug #3: IssueDialog DialogContent auf 810px verbreitert + `max-h-[85vh] flex flex-col`, Tabs-Container auf `flex-1 overflow-hidden` umgestellt. Bug #4: Tab-3-Button "Loesung anwenden" neu gestylt (bg-white/orange-border/green-hover), in eigenem `shrink-0`-Div am Boden fixiert, Content-Bereich scrollbar. Bug #6: WarehouseLocations neue "Artikelnr."-Spalte (falmecArticleNo, sortierbar numerisch), "Artikelnummer" → "Herstellerartikelnr.", Beschreibung auf `descriptionDE ?? descriptionIT` umgestellt. 3 Dateien, tsc 0 Errors. Spec: `PROJ-44-BUGFIX_round2.md`. |
| PROJ-43-44-BUGFIX-R1 | Bugfix Runde 1 (PROJ-43/44 + Lagerort) | In Review | Bug #1: RunDetail Switch-Handler nutzt jetzt setGlobalConfig statt direktem setState — kein Amnesie-Reset bei "Neu verarbeiten" mehr. Bug #2: Sonner z-index 9999 (Toast immer sichtbar ueber Dialogen) + Inline-Feedback "Gespeichert!" am E-Mail-Speichern-Button (2s Timeout). Bug #3: IssueDialog Tab 4 Overflow behoben (flex flex-col h-full + mt-auto am Button-Wrapper). Bug #4: "Loesung erzwingen"-Button Tab 1 neu gestylt (bg-white, orange border, green hover). Bug #5: logicalStorageGroup ('WE'|'KDD'|null) als unveraenderliches Matching-Flag auf InvoiceLine eingefuehrt — WarehouseLocations gruppiert jetzt nach logicalStorageGroup statt storageLocation-Text (kein Amnesie-Drift mehr bei Lagerort-Aenderungen). Bug #6: WarehouseLocations Detailtabelle mit neuer "Pos"-Spalte (positionIndex+1), Default-Sortierung aufsteigend, klickbare TableHead-Header fuer Pos/Lagerort mit ChevronUp/Down-Icons. 9 Dateien, tsc 0 Errors. Spec: `PROJ-43-44_BUGFIX_Runde1.md`. |
| PROJ-46-ADDON | Gedächtnis-Start Bugfix (Klick-to-Mind) | In Review | 3 chirurgische Fixes: (A) Lazy Hydration Guard in `createNewRunWithParsing()` — rehydriert `masterDataStore` aus IndexedDB-restaurierten `uploadedFiles` wenn `articles.length===0`, awaited vor Step-2-Timer-Start, idempotent bei frischem Upload. (B) Pre-Flight-Check in `archiveService.writeEarlyArchive()` — `fileSystemService.hasWriteAccess()` prüft vor `generateArchiveFolderName()`, sofortiger Return `{success:false, reason:'no_permission'}` statt N Einzel-Warnungen; Return-Typ um `reason?:string` erweitert. (C) `.catch()` an `ensureFolderStructure()` in `NewRun.tsx` — kein unhandled Promise rejection mehr. runStore.ts an Archive-Stellen (Z.950-974, Z.1008-1035) unangetastet. tsc 0 Errors. Spec: `PROJ-46-ADDON-Gedaechtnis_Start_Bugfix.md`. |
| PROJ-47 | Blob PDF Viewer (LINK-Sektion) | Done |
| PROJ-44-BUGFIX-Layout | IssueDialog Flexbox-Reparatur | Done | TabsList/Tabs/DialogContent CSS-Klassen bereinigt: `flex-row` auf Tabs, `items-start justify-start` aus TabsList entfernt, `h-fit`+`self-start` als Anti-Stretch. 1 Datei, tsc 0 Errors. Spec: `PROJ-44-BUGFIX_Layout.md`. |
| PROJ-44-ADD-ON-Layout | IssueDialog Horizontal Tab Layout | Done | Umbau von vertikalem Sidebar-Layout auf horizontale Kopfzeilen-Tabs: DialogContent 800px/600px fest, Tabs `flex-col`, TabsList als horizontale border-b-Leiste ohne Sidebar-Hintergrund, TabsContent differenziert (Tab 1/2/5: scroll-container, Tab 3/4: fixed-bottom-button). Reines CSS/Tailwind-Refactoring. 1 Datei, tsc 0 Errors. Spec: `PROJ-44-ADD-ON_Issue-Layout.md`. |
| PROJ-44-ADD-ON-Layout-R2 | IssueDialog Layout-Reparatur Runde 2 | Done | Phase A: Tab-Reiter 3D-Relief-Balken + E-Mail-Tab Side-by-Side. Phase B: Root-Cause-Fix `display:flex` vs Radix `[hidden]` — innere Wrapper statt flex auf TabsContent. Phase C: Icons (Eye/FileText) fuer Tabs Uebersicht+Fehlerbericht, `max-h-[45vh]` aus Fehlerbericht-pre entfernt, E-Mail-Vorschau mit echtem `buildIssueClipboardText`-Output. Phase D: Bugfix `buildIssueClipboardText` — `issue.details` wurde verschluckt wenn affectedLineIds vorhanden + dangling `---` bei leerer Line-Aufloesung. 2 Dateien, tsc 0 Errors. Spec: `PROJ-44-ADD-ON-Layout_round2.md`. |
| PROJ-44-BUGFIX-R3c | Settings-Lock + TabsList Bombproof-Fix | Done | Bug #1 (Settings-Lock): `emailSavedTimerRef` eingefuehrt — `setTimeout(setEmailSaved(false), 2000)` wird jetzt per Ref verwaltet und beim Dialog-Oeffnen gecancelt+zurueckgesetzt. `handleSaveEmails` cancelt Auto-Save-Timer vor manuellem Speichern, synct danach `emailAddresses`-State mit normalisierten Werten (`result.addresses`). `useEffect([open])` loescht beide Timer (autoSave+emailSaved) bei Dialog-Oeffnen. Bug #3 (TabsList IssueDialog): Zusaetzlich zu `h-fit self-start` im className wurde `style={{ height: 'fit-content', alignSelf: 'flex-start' }}` inline gesetzt — Inline-Style schlaegt immer alle CSS-Klassen. 2 Dateien, tsc 0 Errors. | LINK-Sektion in `OverviewPanel.tsx` von 1 auf 3 Buttons erweitert. Button 1: Original-Rechnung öffnen (Eye-Icon, disabled wenn kein File). Button 2: Warenbegleitschein öffnen (Eye-Icon, disabled wenn kein File). Button 3: Archiv im Explorer öffnen (bestehende Logik, Label-Fix). Blob-URL via `URL.createObjectURL()` + 60s Timeout-Revoke (Memory-Leak-Schutz). Rein synchron, kein neuer Service/Hook/Store-Change. 1 Datei, tsc 0 Errors. Spec: `PROJ-47_BLOB-PDF-VIEWER.md`. |


